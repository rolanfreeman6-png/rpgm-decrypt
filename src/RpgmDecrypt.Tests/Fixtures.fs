module RpgmDecrypt.Tests.Fixtures

/// Reserved for future golden fixtures sourced from public-domain
/// freeware RPG Maker games. We deliberately keep this module empty in
/// v0.x rather than vendoring a binary blob in the repository.
let placeholder () = ()
