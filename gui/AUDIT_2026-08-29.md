# AUDIT_2026-08-29 — GUI-интеграция rpgm-decrypt-gui поверх ядра

**Date:** 2026-08-29
**Head commit:** 00828e592dc1b63b357cf74ab08763781ac08bca (v0.3.16)
**Scope:** `gui/**` + все трекаемые файлы репозитория (проверка, что GUI не тронул ядро)
**Methodology:** full-read gui/main.rb, core.rb, build_windows.ps1, test_core.rb, test_run.rb, Gemfile; git diff/status HEAD; SHA-256 побайтовые сверки (ядро↔dist↔zip↔распакованное из exe); рантайм-запуск упакованного exe со снятием stderr.

> **Headline:** Интеграция аккуратная и «нулевого вторжения»: ядро (ocaml/, packaging/, docs/, CI GitLab, LICENSE) байт-в-байт совпадает с HEAD; GUI работает с ядром исключительно как с чёрным ящиком (субпроцесс + NDJSON). Найден 1 important-баг в новом CI-job (упадёт на первом же теге) и несколько minor.

---

## Вердикт по «побайтово не поломано»

| Объект | Проверка | Результат |
|---|---|---|
| `ocaml/`, `packaging/`, `docs/`, `.gitlab-ci.yml`, `CONTRIBUTING.md`, `LICENSE`, `.gitattributes`, `sonar-project.properties` | `git diff` против HEAD | **пусто — байт-в-байт с HEAD** ✅ |
| `ocaml/dist-win/rpgm-decrypt.exe` → `gui/dist/` → zip | SHA-256 | `e955da55bef7…` — **идентичны во всех трёх местах** ✅ |
| `gui/rpgm-decrypt-gui.exe` (упакованный) ↔ `gui/dist/` ↔ zip | SHA-256 | `37556b317c04…` — идентичны ✅ |
| Внутри exe ↔ `gui/main.rb`, `gui/core.rb` | рантайм-распаковка OCRA + SHA-256 | **байт-в-байт идентичны** (main: `eaa811b9…`, core: `fa34b8e9…`) ✅ |
| Изменённые трекаемые файлы | `git status --porcelain` | только `README.md` (+38/−2) и `.github/workflows/release.yml` (+80/−0) — обе правки **аддитивные**, существующие CI-jobs не тронуты |
| Untracked | `git status` | `gui/`, `postman/`, `.postman/` — GUI никогда не коммитился |

**Архитектура выдержана по задумке:** main.rb запускает `rpgm-decrypt.exe` через `Open3.popen3`, парсит NDJSON-прогресс и JSON-summary, маппит пути переименования в собственном `core.rb` (дублирует правило, не трогает ядро). GUI пишет только: temp-каталог ОС, `EXE_DIR/decrypted_cache`, выбранный пользователем output, и папку игры (.bak + замена файла — by design, описано в README). В репозиторий ядра GUI не пишет ничего.

## Верификация заявленных правок агента (все подтверждены)

| Заявление | Файл:строка | Статус |
|---|---|---|
| Заголовок font-weight 600 | main.rb:127 | ✅ |
| Кнопки 500 | main.rb:138 | ✅ |
| Активная вкладка 500 | main.rb:165 | ✅ |
| Обводка кнопок #ccd0d6, без свечения на hover | main.rb:139, :144 (hover — только фон) | ✅ |
| margin: 4px 6px у кнопок | main.rb:142 | ✅ |
| `margin_start = 6` у «Key:» | main.rb:351 | ✅ |
| `.app-frame` оставлен | main.rb:115–121 | ✅ |
| Свечение `.app-title` оставлено | main.rb:124–131 | ✅ |
| «bold» убран | grep | ✅ (осталось слово только в комментарии :157) |
| exe пересобран из нового main.rb | build_check.txt: exe 8/22 7:49:31 PM > main.rb 19:43; строки warning'ов при рантайме совпадают с текущими номерами строк | ✅ |

Тесты: `ruby gui/test_core.rb` → CORE TESTS PASSED; `ruby gui/test_run.rb` → RUN INTEGRATION TEST PASSED (5 NDJSON событий). Рантайм-запуск shipped-exe: стартует, иконки грузятся (нет `[rpgm-gui] * icon load failed`), стили применяются.

---

## IMPORTANT FINDINGS (2)

### I-1 — CI-job `gui-windows` упадёт на чистом checkout: `ocaml/dist-win/` нет в git

**File:** `.github/workflows/release.yml` (новый job, шаг «Build the OCaml decrypter»)
**Severity:** important — первый же пуш тега `v*` уронит релиз GUI-ассета.

`ocaml/dist-win/` игнорируется корневым `.gitignore` (`*.exe`, `*.dll`) и в git отсутствует (`git ls-files ocaml/dist-win` пуст). На раннере после checkout каталога нет, а `Copy-Item _build/default/bin/main.exe ../ocaml/dist-win/rpgm-decrypt.exe -Force` **не создаёт** недостающие каталоги → «Could not find a part of the path». Локально работает только потому, что каталог уже существует.

**Reproducer:** свежий clone → `pwsh: Copy-Item x ../ocaml/dist-win/rpgm-decrypt.exe -Force` → error.
**Fix:** перед копированием добавить
```pwsh
New-Item -ItemType Directory -Force ../ocaml/dist-win | Out-Null
```

### I-2 — в dist/zip подменяется zlib1.dll, парный сборке ядра

**File:** `gui/build_windows.ps1:128,133`; артефакты `gui/dist/zlib1.dll`, zip
**Severity:** important (низкий практический риск, но это не протестированная пара).

`Copy-Item $ZlibDll $dist` (строка 128) кладёт zlib из `ocaml/dist-win` (`1b998ab5…`, v1.3.2), но затем строка 133 заливает поверх весь flatten GTK-рантайма, среди DLL которого свой `zlib1.dll` (`11cd9117…`) — он и остаётся в dist и zip. ABI `zlib1.dll` стабилен, риск совместимости низкий, однако исполняемая пара exe+dll уже не та, с которой ядро собиралось/тестировалось.

**Fix:** переносить `Copy-Item $OcamlExe/$ZlibDll` **после** цикла копирования `$runtime` (или явно перезаписать zlib в конце).

## MINOR FINDINGS (6)

### M-1 — README: из «Project layout» удалена строка `.gitlab-ci.yml`
**File:** README.md (дифф −1 строка). Файл существует, но пропал из схемы. Вернуть строку между `gui/`-блоком и `.github/workflows/`.

### M-2 — мёртвый `GDK_PIXBUF_MODULEDIR` в main.rb:37
Указывает на `lib/gdk-pixbuf-2.0/2.10.0/loaders`, а фактическая раскладка dist — `lib/2.10.0/loaders` (build_windows.ps1:53–56 теряет уровень каталога). Эмпирически иконки в shipped-бандле грузятся (runtime без warnings — gdk-pixbuf находит загрузчики по расположению DLL), так что переменная сейчас просто не работает. Поправить путь или убрать.

### M-3 — zip: обратные слэши в путях + dev-мусор
`Compress-Archive` (PS 5.1) пишет `\` вместо `/` — Windows-распаковщикам ок, строго по спецификации ZIP неверно (запись на linux/macOS unzip даст файлы с `\` в именах; для Windows-only ассета приемлемо). Внутри ~3646 записей: `*.dll.a` (import-библиотеки), `edit.dll`, `libcurl-4.dll`, `libcrypto-3-x64.dll`, `libgnutls-30.dll` и пр., которые GTK3-окну не нужны — zip раздут до 54.9 MB. Чистка фильтром по списку нужных DLL сократила бы архив в разы.

### M-4 — Postman-мусор в корне репо
`postman/` и `.postman/` (Aug 22) — workspace Postman-клиента, к проекту отношения не имеют. Удалить или добавить в `.gitignore`.

### M-5 — косметика рантайма
`Fontconfig error: Cannot load default config file` (безвредно, Win32-шрифтовый бэкенд) и ~20 строк deprecation-warning `Gtk::Box#pack_start` на каждый старт (старый позиционный стиль — GTK3 работает, но шумно; лечится хеш-стилем `{expand:, fill:, padding:}`).

### M-6 — процессный риск: вся GUI-работа не закоммичена
`gui/` — untracked, README/release.yml — modified. Всё живёт только в рабочей копии. Закоммитить (после фикса I-1), иначе потеря при любом `git clean -fdx`.

## Порядок действий

1. Fix I-1 (`New-Item -Force` в release.yml) — иначе первый CI-релиз GUI падает.
2. Fix I-2 (порядок копирования zlib в build_windows.ps1) + пересборка.
3. M-1, M-2, M-4 — по одной строке/правке.
4. Закоммитить `gui/` + README + release.yml.
5. (опционально) M-3 — фильтр DLL, уменьшит zip в разы.

---

## RESOLUTION — 2026-08-29 (все фиксы внесены, пересобрано, проверено)

| ID | Статус | Что сделано |
|---|---|---|
| I-1 | ✅ fixed | release.yml: `New-Item -ItemType Directory -Force ../ocaml/dist-win` перед Copy-Item |
| I-2 | ✅ fixed | build_windows.ps1: exe+zlib ядра копируются в dist ПОСЛЕ flatten GTK-рантайма; после пересборки `gui/dist/zlib1.dll` == `ocaml/dist-win/zlib1.dll` (sha `1b998ab5…`) |
| M-1 | ✅ fixed | README: строка `.gitlab-ci.yml` возвращена в Project layout |
| M-2 | ✅ fixed | main.rb: `GDK_PIXBUF_MODULEDIR` теперь выбирает фактически существующий каталог loaders (`lib/2.10.0/loaders` как fallback) |
| M-3 | ✅ частично | `*.dll.a` исключены из staging (их в dist/zip теперь 0); полный keep-list DLL оставлен на будущее — zip ~54.9 MB |
| M-4 | ✅ fixed | `.postman/`, `postman/` добавлены в корневой .gitignore (сами папки не удалялись) |
| M-5 | ✅ fixed | все 31 позиционных `pack_start` переведены в hash-стиль — 0 deprecation warnings в stderr нового exe |
| M-6 | ✅ fixed | всё закоммичено |

**Must-have функции, добавленные в GUI (все через флаги ядра, архитектура не изменена):**
- **Stop** — общая кнопка остановки (bulk + point); `core.rb.run_decrypt` получил параметр `cancel:` и watcher-поток с `Process.kill('KILL')`; отменённый запуск всегда `success: false` (TerminateProcess на Windows может вернуть код 0); point-режим отличает «cancelled» от «failed».
- **Dry run** — чекбокс `--dry-run` в bulk-вкладке.
- **Password-file** — четвёртый пункт комбо ключа (`--password-file` со списком кандидатов).
- **Open output folder** — кнопка в bulk-вкладке; `open_path` теперь корректно открывает сам каталог (и без Shellwords-искажения путей).
- **Save log...** — экспорт лога в файл через FileChooserDialog.
- **Подтверждение непустой выходной папки** — модальный yes/no диалог перед запуском bulk.

**Верификация после пересборки (все шаги пройдены):**
- `ruby gui/test_core.rb` PASS; `ruby gui/test_run.rb` PASS + новый adversarial cancel-тест (kill за 0.6с).
- Ядро не тронуто: `git diff ocaml/ packaging/ docs/` по-прежнему пуст; `rpgm-decrypt.exe` sha `e955da55…` без изменений.
- Цепочка хэшей: `gui/rpgm-decrypt-gui.exe` == `gui/dist/…` == внутри zip (`887f16ba…`); zlib в dist и zip == ядерная пара (`1b998ab5…`).
- Рантайм: exe стартует, окно отрисовывается (скриншот: все новые контролы на месте), stderr чист (0 deprecations), встроенные `main.rb`/`core.rb` SHA-256 идентичны исходникам (`331bc151…` / `f87876da…`).
- GTK dialog API (FileChooserDialog/MessageDialog/pack_start hash) проверены отдельным smoke-скриптом до сборки.

---

## ADDENDUM — Star Gate (2026-08-29, вторая итерация)

**Запрос:** при первом запуске GUI требует поставить ★ репозиторию на GitHub (порт проверки из RenpyEx `src/gui/star_gate.rs`).

**Реализация** (`gui/star_gate.rb` + `GateWindow` в `main.rb` + `gui/test_gate.rb`):
- GitHub OAuth **Device Flow** (client_id, пустой scope → токен читает только публичный профиль; секрет не нужен и не зашивается).
- Проверка звезды: `GET /user/starred/rolanfreeman6-png/rpgm-decrypt` с токеном → **204 = звезда есть**; 404 = нет. Идентичность подтверждается входом в браузере — чужим ником пройти нельзя (анонимные endpoints звезды GitHub закрыл в 2025).
- Активация пишется в `config.json` (`starred_by`) рядом с exe → **навсегда на этой машине, включая офлайн**; токен только в памяти, на диск не пишется. Best-effort, не DRM.
- UI: экраны Intro → 3 шага (device code, открытие браузера) → «Almost there, <login>!» (кнопка Star + Check again) → Unlocked. Стили .app-frame/.app-title переиспользованы (`apply_style` стал class-method).
- Бутстрап: `load_unlock(EXE_DIR)` → есть `starred_by` ⇒ сразу `RpgmGui.new`; нет ⇒ `GateWindow`, и главный GUI открывается только после верификации (`gate.unlocked?`).
- Ошибки сети/отказ: экран Retry с пометкой «интернет нужен только при первой активации».

**Верификация:**
- `test_gate.rb` (офлайн-юниты): контракт событий `run_device_flow` (device_code→starred, denied-путь), классификация poll-ответов, round-trip `save_unlock`/`load_unlock` (включая битый JSON — не роняет загрузку), связность REPO_URL/OWNER_REPO, формат client_id — **PASS**.
- LIVE: `request_device_code` получил реальный код от GitHub (доказывает: OAuth app жив, Device Flow включён, сеть работает).
- Рантайм собранного exe: первый запуск (без config.json) → gate-окно ✓; с `config.json` → сразу главное окно ✓; встроенные `main.rb`/`star_gate.rb` SHA-256 идентичны исходникам ✓; хэш-цепочка exe==dist==zip ✓; zlib в zip — ядерная пара ✓; ядро `ocaml/` не тронуто (e955da55…) ✓.
- Полный клик-тест гейта человеком и подмена client_id на выделенный «rpgm-decrypt GUI» app — после регистрации OAuth app владельцем (см. ниже).

**Открытый пункт:** в бандле сейчас client_id приложения **RenpyEx** (переиспользован для smoke). Владелец регистрирует dedicated OAuth app «rpgm-decrypt GUI» (Device Flow enabled, redirect `http://localhost`, wildcard/expire — off); после замены константы `OAUTH_CLIENT_ID` и пересборки на странице согласия будет правильное имя приложения.

---

## ADDENDUM 2 — Star Gate доведён до продакшена (2026-08-29, финал)

**OAuth:** зарегистрирован выделенный app «rpgm-decrypt GUI», client_id `Ov23liyHPi1XYLX3hrQB` вшит в `star_gate.rb` (секрет не нужен). Live-проверка device code — OK.

**Пойманные и исправленные баги на реальных запусках:**
1. **Краш по кнопке Sign-in** — `Thread#daemon=` не существует в Ruby (перенесено из Rust по памяти). Убрано; в Ruby воркеры и так умирают с главным потоком.
2. **TLS в packed exe: HTTP 0** — GTK-рантайм тащит свои `libcrypto-3-x64.dll`/`libssl-3-x64.dll`, которые затеняли ruby_builtin_dlls-версии (поиск DLL: app dir первым), а их скомпилированные пути к CA-сертификатам не существуют. Фикс: обе DLL исключены из staging; в бандл добавлен `cacert.pem`; `main.rb` ставит `SSL_CERT_FILE` до любого TLS. Ошибки сети теперь показывают реальную причину, а не «HTTP 0».
3. **Белый экран после Sign-in** — `GLib.markup_escape_text` не существует в ruby-gnome 4.x → `show_device` падал после `clear` и оставлял пустое окно. Заменено на локальный `xml_escape`; любое падение рендера теперь деградирует в экран ошибки с Retry, а не в белую пустоту.
4. **Главное окно не открывалось после Unlocked** (процесс умирал молча) — окно создавалось после `Gtk.main_quit`. Переделано на один `Gtk.main` для всего приложения: gate-окно при destroy внутри того же цикла передаёт эстафету `RpgmGui.new`. Покрыто автотестом `test_gate_transition.rb` (инжекция `:starred` без сети).
5. **Фриз окна при открытии браузера** — `system('cmd /c start …')` блокировал GTK main loop на время старта браузера. Заменено на `Process.spawn` + `detach` (и в `open_path` главного окна тоже).

**Финальная верификация (упакованный exe `011c9484…`):**
- Живой полный цикл владельцем: Intro → Sign-in → device code → браузер → authorize → (звезда уже стоит) → Unlocked → главное окно; `config.json` записан (`starred_by: rolanfreeman6-png`); повторный запуск открывает главное окно сразу, без сети.
- Встроенные `main.rb`/`star_gate.rb`/`core.rb` SHA-256 идентичны исходникам (свежее извлечение OCRA).
- Тесты: `test_gate.rb`, `test_gate_transition.rb`, `test_core.rb`, `test_run.rb` — все PASS.
- Хэш-цепочка exe==dist==zip; ядро `e955da55…` не тронуто; zip ~50 MB.
- Билд-скрипт теперь чистит `libcrypto/libssl` из staging и бандлит CA-сертификаты — воспроизводимо в CI.

**Известные границы (by design):** активация хранится в `config.json` рядом с exe — удалив файл, пользователь активирует заново (это «вежливая просьба», не DRM); первый запуск требует интернет; гейт можно обойти правкой конфига — против этого сознательно не защищаемся.

---

## ADDENDUM 3 — Аудит GUI-кода + живой прогон на SaihateekiDemo (2026-08-29)

Полное перечитывание всех GUI-файлов (main.rb 950 строк, star_gate.rb, core.rb, build_windows.ps1) по методике аудит-линз + реальная расшифровка игры `D:\SteamLibrary\steamapps\common\SaihateekiDemo` (RPG Maker MV, NW.js; 394 `.png_` + 361 `.ogg_`, ключ `d41d8cd9…ecf8427e` из System.json).

### C-1 (critical, в бандле, не в коде) — дистрибутивное ядро отставало от источника

**Файл:** `ocaml/dist-win/rpgm-decrypt.exe` (был e955da55…, v0.3.2, от 30 июня)

Источник ядра — v0.3.16, `--assets-only` появился в 0.3.13 (c5846f7), но в dist-win лежал exe 0.3.2, который на этот флаг отвечал usage error (exit 2). Следствие: вкладка «Single file» в GUI всегда падала сразу после выбора файла, а чекбокс «Assets only» в «Whole game» ломал запуск. Побайтовая сверка GUI-скриптов это не ловит — GUI был корректен, ядро устарело. dist-win exe никогда не коммитился, поэтому CI собирал из источника правильный бинарник, а локальная сборка бандла — нет.

**Фикс:** ядро пересобрано из нетронутого источника (`dune build --profile release`, opam-switch default, OCaml 5.5.0; для сборки понадобились mingw-binutils из `C:\msys64\ucrt64\bin` + шимы префиксных имён `x86_64-w64-mingw32-{ar,ld,as,…}` в PATH — `dune` зовёт их по префиксу). Новый exe `134847be…`, тесты ядра: 92/92 + 12/12 QCheck. zlib1.dll не менялся (1b998ab5…).

### I-1 — нажатие клавиши в поле фильтра перезапускал полный рескан папки

**Файл:** `gui/main.rb` (build_point_tab). `@filter.signal_connect('changed') { refresh_list }` — на каждый символ проход `Find.find` по всему дереву игры с пересозданием `@files`. На папке в тысячи файлов — заметные фризы UI. Фикс: фильтр теперь только перефильтровывает уже просканированный список (`populate_tree`), рескан — по кнопке Refresh/выбору папки.

### I-2 — ключ/пароль писались в лог открытым текстом

**Файл:** `gui/main.rb` (run_decrypt → `log(args.shelljoin)`). Значения `--password`/`--vxace-seed` попадали в лог-буфер и в сохранённый файл лога. Фикс: `RpgmGui.redact_join` маскирует значения как `REDACTED` (путь из `--password-file` не секрет и остаётся виден). Попутно убран сам `shelljoin`: он съедал обратные слэши Windows-путей (`C:\keys.txt` → `C:keys.txt`), лог был искажён; спавн и так идёт по массиву аргументов.

### I-3 — необработанные исключения в файловых операциях роняли GTK main loop

**Файл:** `gui/main.rb` (point_decrypt on_done, return_to_place). `FileUtils.cp` без rescue: нет прав/файл занят → исключение внутри `GLib::Idle`/сигнала — аварийное завершение приложения вместо сообщения в лог. Фикс: оба блока обёрнуты в begin/rescue с логом ошибки; temp-каталог чистится в `ensure` даже при падении.

### I-4 — GUI позволял выбрать выходную папку = папке игры

Ядро не проверяет `out_dir == game_dir` (зеркальный режим перезаписал бы оригиналы на месте). Фикс в `run_bulk`: гард с сообщением, что выходная папка должна отличаться от исходной.

### Живой прогон на SaihateekiDemo (ядро 0.3.16 из бандла)

- **Single-file режим (та же командная строка, что шлёт GUI):** `--assets-only --log-format json --report-format json` → exit 0, `scanned=804 decrypted=755 passthrough=49 failed=0`, ключ найден автоматически (`key_source: System.json`), NDJSON-события 1:1 с тем, что парсит core.rb (walked/detected/key_found/decrypt/passthrough).
- **Все 755 расшифрованы:** 443 PNG + 361 OGG; выборочно и повально проверены magic-байты (`89504e47…`, `4f676753`), ни одного файла с зашифрованным расширением не осталось; заглавный `noizu_2.png` 960×540 — 382 чанка, все CRC валидны, IDAT разжимается (2 074 613 байт пикселей) — неверный ключ дал бы CRC-ошибки.
- **Whole game (зеркало на C:):** exit 0, те же счётчики; дерево каталогов 1:1 с исходником (diff пустой), файлов 1282/1282, Game.exe на месте, 811 МБ; в зеркале `System.json` вычищен (`hasEncryptedImages/Audio: false`) — decrypted-копия готова к запуску; 5/5 случайных PNG из `img/**` CRC-валидны.

**Верификация фиксов:** ruby -c OK; test_run.rb, test_gate.rb, test_gate_transition.rb — PASS; юнит-тест redact_join — OK; бандл пересобран (OCRA), встроенные скрипты = исходникам (извлечение из живого процесса), ядро в dist/zip = 134847be….

### ADDENDUM 3.1 — Раскладка окна (по фидбеку владельца)

Пустая зона под «Decrypt game» была фундаментальным поведением `Gtk::Notebook`: он резервирует высоту самой высокой вкладки (Single file ~458px против Whole game ~262px), и в короткой вкладке остаётся пустота. Заменено на `Gtk::Stack` + `Gtk::StackSwitcher` с `vhomogeneous=false`: размер берётся только с активной страницы, лог (упакован с `expand: true`) растягивается на всё освободившееся место. CSS вкладок переписан под StackSwitcher (active = `:checked`). Дефолт окна 960×780. Побочно починен `test_gate_transition.rb` (лишний второй `Gtk.main` мог зависать). Подтверждено владельцем визуально на собранном exe.

### ADDENDUM 3.2 — Падение CI: фантомный fiber.so в OCRA + пропатченный локальный гем

**Симптом (3 прогона):** CI-джоба GUI падала на OCRA с exit 1 сразу после «Adding user-supplied source files», без ERROR-строки и бэктрейса в конце лога. Первая гипотеза — Windows Defender на раннере убивал ruby (silent exit). Исключения Defender + retry не помогли: полный лог показал, что реальная ошибка печаталась *раньше* в логе и была детерминированной:

```
ocra:1188: No such file or directory @ rb_sysopen - <gui>/fiber.so (Errno::ENOENT)
```

**Корень (два слоя):**
1. Ruby 3.3.12 держит `fiber.so` в `$LOADED_FEATURES`, хотя файла нигде нет (фича встроена в интерпретатор). OCRA 1.3.11 знает фантомы старых руби (`IGNORE_MODULE_NAMES`: enumerator.so, rational.so, complex.so, thread.rb, ruby2_keywords.rb), но fiber.so в список не входит: не найдя файл ни в одном load path, он разрешает его относительно корня исходников и пытается `File.open "<script dir>/fiber.so"`. Файла нет → ENOENT → `raise` (fiber.so не в rescue-списке) → exit 1.
2. Локально сборка работала только потому, что локальный гем OCRA был **вручную пропатчен** строкой `return unless File.exist?(src)` в `OcraBuilder#createfile` (сделано в прошлой сессии, из контекста потерялось). CI ставил чистый гем с rubygems — и падал.

**Фикс:** `gui/tools/patch_ocra.rb` — идемпотентный патчер, добавляющий тот же guard в `createfile` (байт-точно, CRLF сохранены, якорь `ensuremkdir(tgt.dirname)` + `str = File.open(src`; при изменении раскладки гема — громкий abort). `build_windows.ps1` вызывает его перед OCRA, поэтому локальная и CI-сборки ведут себя одинаково независимо от состояния гема.

**Верификация:** локальный гем возвращён в pristine → патчер применил guard → полный `build_windows.ps1` собрал exe первого захода (9 186 412 байт, байт-в-байт прежний размер), смоук exe OK, зип: 2918 записей, 0 backslash-имён, одна папка `rpgm-decrypt-gui/`, README.txt внутри, утечек (config.json/gtk_runtime) нет.
