/* No-op stub. bisect-ppx-report crashed before writing its coverage.js on this
   Windows build (see coverage.css header). The per-file pages reference it; a
   no-op is sufficient — the navbar markers are positioned inline via style. */
